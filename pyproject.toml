[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "needs_api: Marks a test that needs the actual AlephAlpha API endpoint"
]
asyncio_mode = "auto"